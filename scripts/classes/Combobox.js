class Combobox {
    constructor(elementId, keyword) {
        this.element = document.getElementById(elementId)

        //this.list = définir la filterList (SET dans classe Search)

        //this.input = récupérer l'input => modifier mon HTML

    }

    // displayCombobox(){

    // }
}